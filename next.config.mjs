/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  images: {
    domains: [
      "api.edu.assaab.uz",
      "admin.it-ea.uz",
      "encrypted-tbn0.gstatic.com",
    ],
  },
};

export default nextConfig;
